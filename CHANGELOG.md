# 変更履歴

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に沿います。

リリースノートはこのファイルの該当バージョンの節から自動で作られます
（`.github/workflows/release.yml`）。

## 0.1.0

最初のリリース。実機のサーマルプリンタでの印字はまだ検証していないため、
0.x として公開します。

### デスクトップ版（Windows / macOS）

- **テキスト印刷** — 定型文（受付メモ / TODO / 電話メモ / 申し送り）、見出し・日時・罫線、
  禁則処理つきの折り返し。`Ctrl+Enter` でそのまま印刷
- **スクショ印刷** — OS 標準の範囲選択（Windows の切り取り＆スケッチ / macOS の
  `screencapture -i`）、アプリ内蔵のオーバーレイ選択、画面全体、クリップボード貼り付け。
  二値化 3 方式（ディザ / しきい値 / 適応的）と明るさ・コントラスト・幅をスライダで調整
- **ファイル取り込み** — PDF・Word・テキスト・画像を「サムネイル」か「テキスト抽出」で。
  ページ指定（`1,3-5`）、OCR、抽出後の手直しに対応
- **QR 印刷** — 任意の URL・テキストを QR に。Google ドライブへアップロードして
  そのリンクの QR を出す導線つき。誤り訂正の既定は Q
- **履歴** — SQLite に全印刷を保存し、検索・再印刷・PNG 書き出し・削除
- **アーカイブ** — 同期フォルダへ Markdown + PNG、または印刷のたびに自分宛メール
  （Gmail のフィルタでラベル自動付与）
- **自動更新** — GitHub Releases を見て新版を通知し、インストーラを取得して適用
- **CLI** — `print-text` / `print-file` / `test` / `test-page`

### Android 版

- 他アプリの「共有」から直接印刷: テキスト、画像・スクショ、PDF、複数画像
- テキスト選択メニューからの印刷（`ACTION_PROCESS_TEXT`）
- 端末内の印刷履歴（検索・再印刷・プレビュー・削除）
- 自動更新（GitHub Releases から APK を取得して検証のうえインストール）
- AndroidX を含む外部ライブラリは不使用。APK は約 60KB

### 共通

- 日本語はすべて画像化して送るため、プリンタ内蔵フォントやコードページに依存しない
- ESC/POS ラスタ（`GS v 0`）を既定 128 行ずつ分割送信
- Android 版の ESC/POS 生成が Python 版とバイト単位で一致することを CI で検証

### 既知の制限

- 実機のサーマルプリンタでの印字は未検証
- Android 版の PDF はページ画像のみ（標準 API にテキスト抽出が無いため）
- Android 版の履歴はクラウド同期に未対応
- macOS のアプリは署名していないため、初回は右クリック →「開く」が必要
- Intel Mac 向けの配布物はなし（GitHub が Intel macOS ランナーの提供を終了したため）。
  該当する場合はソースから起動してください
