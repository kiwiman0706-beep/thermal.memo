# --- 必須 ---
Pillow>=10.0            # 画像生成・二値化（テキストの画像化にも使用）

# --- 任意（無くても該当機能だけ無効化されて動きます）---
PyMuPDF>=1.23           # PDF サムネイル & テキスト抽出（推奨）
pdfplumber>=0.10        # PDF テキスト抽出のフォールバック
python-docx>=1.1        # .docx テキスト抽出
opencv-python>=4.8      # 適応的二値化（adaptive threshold）
numpy>=1.24             # 画像パック高速化 / opencv 連携
pytesseract>=0.3        # 画像PDF・スクショの OCR（Tesseract 本体も必要）
tkinterdnd2>=0.4        # ウィンドウへのファイル D&D
